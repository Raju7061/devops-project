apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debezium.fullname" . }}-test-connection"
  labels:
    {{- include "debezium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debezium.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
